/* Dummy — overridden by user.ld for userspace builds.
 * This file exists because Cargo merges .cargo/config.toml from parent
 * directories, and the kernel's config adds -Tkernel/linker.ld.
 * The real layout comes from user.ld in this crate.
 */
