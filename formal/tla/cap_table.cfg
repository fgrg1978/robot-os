SPECIFICATION Spec

CONSTANTS
    NumSlots = 3
    MaxGen   = 3
    NumOps   = 8

INVARIANT TypeOK
INVARIANT AtMostOneValidPerSlot
INVARIANT RevokedNeverValid
INVARIANT GenInRange

\* The model halts naturally when `opCount == NumOps`; TLC's default
\* deadlock check then triggers a false-positive. Disable it.
CHECK_DEADLOCK FALSE
