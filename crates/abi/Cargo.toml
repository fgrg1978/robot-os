[package]
name = "robot_os_abi"
version = "1.0.0"
edition = "2021"
description = "PHANES frozen ABI — syscall numbers, error codes, repr(C) types crossing the user/kernel boundary. RFC-0008. Stable v1.x."
license = "Apache-2.0"
repository = "https://github.com/phanes-project/phanes"

[lib]
name = "robot_os_abi"

[dependencies]

[features]
default = []
