[package]
name = "robot_os_sched_policy_tests"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"
description = "Host-side tests for the W4 multi-policy scheduler. Excluded from workspace."

# Pulls crates/sched/src/{class,partitions,policies/*}.rs in via #[path]
# so the embedded #[cfg(test)] suites + integration tests run on the
# developer host.
#
# Run with:  cd crates/sched-policy-tests && cargo +stable test

[dependencies]

[dev-dependencies]
