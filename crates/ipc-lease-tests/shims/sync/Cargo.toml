[package]
name = "ipc_lease_test_sync"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"
description = "Host stand-in for robot_os_sync, used only by crates/ipc-lease-tests. NOT part of the kernel."

[dependencies]
