[package]
name = "robot_os_arch_api"
version = "0.1.0"
edition = "2021"
description = "PHANES Arch trait surface — cross-ISA abstraction for Cpu / Interrupts / Mmu / Boot / Vector. RFC-0002 modular pattern. Pure no_std, host-compatible."
license = "Apache-2.0"
repository = "https://github.com/phanes-project/phanes"

[lib]
name = "robot_os_arch_api"

[dependencies]
