[package]
name = "topology_seed_test_sched"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"
description = "Host stand-in for robot_os_sched's task-pool lookup, used only by crates/topology-tests' P1 cap-seed bridge suite. NOT part of the kernel."

[dependencies]
robot_os_limits = { path = "../../../limits" }
