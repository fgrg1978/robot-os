[package]
name = "robot_os_msc"
version = "0.1.0"
edition = "2021"
description = "DEV03 — USB Mass Storage Class (MSC) Bulk-Only Transport + minimal SCSI command set. Pure no_std protocol layer; the USB device controller surface lives in robot_os_drivers::usb_device."
license = "Apache-2.0"

[lib]
name = "robot_os_msc"
