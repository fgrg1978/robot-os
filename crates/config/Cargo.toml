[package]
name = "robot_os_config"
version = "0.1.0"
edition = "2021"

# Pure no_std — zero external dependencies.
# cfg_load is called once during single-threaded boot; all other
# functions run from the shell task (no concurrent writers).
[dependencies]
